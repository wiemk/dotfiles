# gets sourced by .zshrc after .zshrc.pre, has to be treatet as .zshrc

# config dropins
for i ("$ZDOTDIR"/.zsh.d/*.zsh(N)); do
    if [[ -r "$i" ]]; then
        source "$i"
    fi
done
unset i

# EOF
# vim :set ts=4 sw=4 sts=4 et :
