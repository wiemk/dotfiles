# profile-electra sourced by ~/.profile
DOTFILES=$HOME/repos/working/dotfiles
PATH=$PATH:$HOME/.local/share/node_modules/.bin

# debug
if [[ -e "${XDG_CONFIG_HOME}/profile/_debug" ]]; then
	echo "$(date +%s): profile-electra" >> "${HOME}/shell_debug.log"
fi

# EOF
