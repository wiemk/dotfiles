# profile-zenode
DOTFILES=$HOME/repos/working/dotfiles
# learn something while we wait for zplug
#command -v fortune >/dev/null 2>&1 && env fortune -a
command -v archey3 >/dev/null 2>&1 && env archey3

# debug
if [[ -e "${XDG_CONFIG_HOME}/profile/_debug" ]]; then
	echo "$(date +%s): profile-zenode" >> "${HOME}/shell_debug.log"
fi

# vim: set ft=sh ts=4 sw=4 sts=0 tw=0 noet :
# EOF
