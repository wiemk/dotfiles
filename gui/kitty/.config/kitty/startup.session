new_tab
launch
new_tab split
launch
launch
launch
new_tab bash
launch /usr/bin/bash -i
new_tab toolbox
launch /usr/bin/toolbox enter
new_tab tmux
launch /usr/bin/tmux new-session -A -s main
new_tab ssh
# work around a data race issue
launch sh -c 'sleep 0.1s; /usr/bin/tmux new-session -A -s ssh'

